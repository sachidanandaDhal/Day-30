if both are run at the same time then you can use the
**IMP**
always // Call the function from `datatype.go`
    DescribeDataTypes()

   ********** RUN IMPORTANT ********************************
go run 1.go datatype.go